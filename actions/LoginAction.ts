import { FormActionState } from "@/types/FormActionState";
export default async function Action(state:any,formData:FormData){
    let response = {status:null,message:null,className:null}

    return response;
}